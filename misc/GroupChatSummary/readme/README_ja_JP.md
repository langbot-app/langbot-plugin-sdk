# GroupChatSummary

LLM を使用してグループチャットのメッセージを要約します。

## コマンド

| コマンド | 説明 |
|---------|------|
| `!summary [数]` | 最近の N 件のメッセージを要約（デフォルト 100） |
| `!summary hours <N>` | 過去 N 時間のメッセージを要約 |
| `!summary status` | メッセージバッファの状態を表示 |
| `!summary clear` | 保存されたメッセージをクリア |
