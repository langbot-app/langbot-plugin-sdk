# HelloPlugin

这是一个实验性插件，提供了 `EventListener`、`Tool` 和 `Command` 组件的演示示例。

- `components/event_listener/default.py`：当收到个人消息时简单地回复一条消息。
- `components/tools/get_weather_alerts.py`：提供一个可用于获取美国各州天气预警信息的工具。
- `components/commands/info.py`：响应 `info` 命令并返回命令执行的上下文信息。
