# URL サマリー

チャットで共有されたWebページのリンクを自動的に要約します。

リンクが送信されると、プラグインがWebページの内容を取得し、LLMを使用して簡潔な要約を生成します。

## 機能

- メッセージ内のURLを自動検出（プライベートチャットとグループチャットの両方）
- HTMLページから読みやすいテキストを抽出
- LLMを使用して簡潔な要約を生成
- 多言語サポート（中国語、英語、日本語）
- コンテンツの長さとモデルの選択が設定可能
- メッセージごとに最大3つのリンクを処理
